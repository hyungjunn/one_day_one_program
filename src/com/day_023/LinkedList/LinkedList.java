package com.day_023.LinkedList;

//class LinkedList { // 삭제하고자 하는 요소의 이전 요소가 삭제하고자 하는 요소의 다음 요소를 참조하도록 변경하기만 하면 된다.
//    node(vertex) 값value과 다음 노드의 위치next를 가지는 객체
//    head는 LinkedList의 첫번째 node를 가리킨다.

//    각 node는 값value와 다음 노드로 연결되는 포인터(링크)next를 가진다.
//    노드의 종류는 head와 일반 원소가 되는 node가 있다.
//    head는 값은 가지지 않으며 항상 LinkedList의 첫번째 node를 가리킨다.
//    node는 다음 node가 없는 경우 null을 가리킨다.

//    ListNode head;
//
//    ListNode add(ListNode head, ListNode nodeToAdd, int position) { // position == index
//        // 지정된 position에서
//        // node추가
//    }
//
//    ListNode remove(ListNode head, int positionToRemove) {
//
//    }
//
//    boolean contains(ListNode head, ListNode nodeTocheck) {
//        //  지정된 객체가 LinkedList에 포함되었는지 안되어있는지
//    }
//
//
//}
